import {

} from '../actions/types';
import {Action} from "redux";

const defaultState = {

};

const reducer = (state = defaultState, action: Action) => {
	switch (action.type) {
		case '': {
		
		}
	}
	
	return state;
};

export default reducer;
